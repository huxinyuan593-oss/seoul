{
  "name": "btc-matching-engine",
  "version": "0.1.0",
  "description": "BTC Matching Engine — in-memory Order Book + UTXO locking + idempotency",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/order-service.js",
    "test": "jest --verbose"
  },
  "dependencies": {
    "ioredis": "^5.4.1",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0",
    "@types/uuid": "^10.0.0",
    "jest": "^29.7.0",
    "@types/jest": "^29.5.0",
    "ts-jest": "^29.2.0"
  }
}
